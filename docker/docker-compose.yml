version: "3.8"

services:
  web:
    build: ../app
    container_name: flask_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DEBUG=True
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
    networks:
      - app-network

networks:
  app-network:
